import React from 'react'
import Posts from './posts';


function Favourites() {
    return (
      // Shows only posts in which is favourited by the current user
      <Posts />
    )
}

export default Favourites